diff --git a/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/shell/ShellCommandSafetyPolicy.kt b/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/shell/ShellCommandSafetyPolicy.kt
index 3ffc43f..0000000 100644
--- a/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/shell/ShellCommandSafetyPolicy.kt
+++ b/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/shell/ShellCommandSafetyPolicy.kt
@@ -39,6 +39,15 @@ class ShellCommandSafetyPolicy(
         when (val builtIn = candidates.evaluateBuiltInProtection()) {
             is BuiltInProtection.Deny -> {
+                SecurityAuditLog.record(
+                    kind = SecurityAuditKind.POLICY_BLOCKED,
+                    riskLevel = SecurityRiskLevel.CRITICAL,
+                    toolName = toolName,
+                    ruleName = builtIn.name,
+                    policyCode = "BUILTIN_CRITICAL_BLOCKED",
+                    reason = builtIn.reason,
+                    command = command,
+                )
                 return ShellCommandPolicyDecision(
                     allowed = false,
                     code = "BUILTIN_CRITICAL_BLOCKED",
@@ -57,6 +66,9 @@ class ShellCommandSafetyPolicy(
                             toolName = toolName,
                             command = command,
                             matchedRuleName = builtIn.name,
+                            riskLevel = SecurityRiskLevel.HIGH,
+                            riskReason = builtIn.reason,
+                            temporaryGrantMillis = 5 * 60 * 1000L,
                         )
                     )
                 ) {
@@ -155,6 +167,9 @@ class ShellCommandSafetyPolicy(
             toolName = toolName,
             command = command,
             matchedRuleName = matchedRuleName.orEmpty(),
+            riskLevel = SecurityRiskLevel.HIGH,
+            riskReason = reason,
+            temporaryGrantMillis = 5 * 60 * 1000L,
         )
     }
@@ -174,6 +189,7 @@ class ShellCommandSafetyPolicy(
                 return BuiltInProtection.Confirm(
                     name = rule.name,
                     pattern = rule.label,
+                    reason = rule.reason,
                 )
             }
         }
@@ -278,6 +294,7 @@ class ShellCommandSafetyPolicy(
         data class Confirm(
             val name: String,
             val pattern: String,
+            val reason: String,
         ) : BuiltInProtection
@@ -341,7 +358,7 @@ class ShellCommandSafetyPolicy(
             BuiltInPattern(
                 name = "Sensitive: file mutation",
                 label = "rm/mv/chmod/chown/chgrp",
-                reason = "",
+                reason = "This command can modify, move, delete, or change access to files.",
                 regex = Regex("\\b(?:rm|mv|chmod|chown|chgrp)\\b", RegexOption.IGNORE_CASE),
             ),
             BuiltInPattern(
@@ -349,7 +366,7 @@ class ShellCommandSafetyPolicy(
             BuiltInPattern(
                 name = "Sensitive: app/package management",
                 label = "pm/cmd package/appops/dpm",
-                reason = "",
+                reason = "This command can install, remove, disable, or change permissions for apps.",
                 regex = Regex("\\b(?:pm|appops|dpm)\\b|\\bcmd\\s+package\\b", RegexOption.IGNORE_CASE),
             ),
             BuiltInPattern(
@@ -357,7 +374,7 @@ class ShellCommandSafetyPolicy(
             BuiltInPattern(
                 name = "Sensitive: system settings",
                 label = "settings/setprop",
-                reason = "",
+                reason = "This command can change Android system settings or system properties.",
                 regex = Regex("\\b(?:settings|setprop)\\b", RegexOption.IGNORE_CASE),
             ),
             BuiltInPattern(
@@ -365,7 +382,7 @@ class ShellCommandSafetyPolicy(
             BuiltInPattern(
                 name = "Sensitive: process/device control",
                 label = "kill/reboot/shutdown/force-stop",
-                reason = "",
+                reason = "This command can stop apps or processes, reboot, or power off the device.",
                 regex = Regex("\\b(?:kill|pkill|killall|reboot|shutdown|poweroff)\\b|\\bam\\s+force-stop\\b", RegexOption.IGNORE_CASE),
             ),
             BuiltInPattern(
@@ -373,7 +390,7 @@ class ShellCommandSafetyPolicy(
             BuiltInPattern(
                 name = "Sensitive: privilege escalation",
                 label = "su/magisk",
-                reason = "",
+                reason = "This command requests elevated privileges and can bypass normal Android app isolation.",
                 regex = Regex("(?:^|\\s)(?:su|magisk)(?:\\s|$)", RegexOption.IGNORE_CASE),
             ),
             BuiltInPattern(
@@ -381,7 +398,7 @@ class ShellCommandSafetyPolicy(
             BuiltInPattern(
                 name = "Sensitive: network transfer",
                 label = "curl/wget",
-                reason = "",
+                reason = "This command can send data to or download data from an external network endpoint.",
                 regex = Regex("\\b(?:curl|wget)\\b", RegexOption.IGNORE_CASE),
             ),
             BuiltInPattern(
@@ -389,7 +406,7 @@ class ShellCommandSafetyPolicy(
             BuiltInPattern(
                 name = "Sensitive: network policy",
                 label = "iptables/nft/ip route/ip rule",
-                reason = "",
+                reason = "This command can alter routing, firewall, or other device network policy.",
                 regex = Regex("\\b(?:iptables|ip6tables|nft)\\b|\\bip\\s+(?:route|rule)\\b", RegexOption.IGNORE_CASE),
             ),
         )
diff --git a/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/buildin/impl/TerminalBuiltin.kt b/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/buildin/impl/TerminalBuiltin.kt
index 9f1651f..0000000 100644
--- a/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/buildin/impl/TerminalBuiltin.kt
+++ b/agent-runtime/src/main/java/com/niki914/zafiro/chat/agentic/buildin/impl/TerminalBuiltin.kt
@@ -111,6 +111,7 @@ class TerminalBuiltin(
                         command = command,
                         matchedRuleName = "Privileged identity: $identity",
                         temporaryGrantMillis = 5 * 60 * 1000L,
+                        riskReason = "$identity execution runs with elevated device privileges and can access data or settings unavailable to a normal app process.",
                     )
                 )
                 when (response) {
diff --git a/app/src/main/java/com/niki914/zafiro/app/ui/content/HomePageContent.kt b/app/src/main/java/com/niki914/zafiro/app/ui/content/HomePageContent.kt
index 3727108..0000000 100644
--- a/app/src/main/java/com/niki914/zafiro/app/ui/content/HomePageContent.kt
+++ b/app/src/main/java/com/niki914/zafiro/app/ui/content/HomePageContent.kt
@@ -405,6 +405,23 @@ private fun ToolPermissionDialog() {
                     style = MaterialTheme.typography.bodySmall,
                     color = MaterialTheme.colorScheme.error,
                 )
+                Text(
+                    text = stringResource(
+                        R.string.tool_permission_risk_level,
+                        request.riskLevel.name
+                    ),
+                    style = MaterialTheme.typography.bodySmall,
+                    color = MaterialTheme.colorScheme.error,
+                )
+                request.riskReason?.takeIf { it.isNotBlank() }?.let { reason ->
+                    Text(
+                        text = stringResource(R.string.tool_permission_risk_reason, reason),
+                        style = MaterialTheme.typography.bodySmall,
+                        color = MaterialTheme.colorScheme.onSurfaceVariant,
+                    )
+                }
             }
         },
         actions = {
diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
index 5d90721..0000000 100644
--- a/app/src/main/res/values/strings.xml
+++ b/app/src/main/res/values/strings.xml
@@ -215,6 +215,8 @@
     <string name="tool_permission_dialog_title">AI 请求执行操作</string>
     <string name="tool_permission_request_intro">工具「%1$s」请求执行以下命令：</string>
     <string name="tool_permission_matched_rule">命中规则「%1$s」</string>
+    <string name="tool_permission_risk_level">风险等级：%1$s</string>
+    <string name="tool_permission_risk_reason">风险原因：%1$s</string>
     <string name="tool_permission_allow">允许本次</string>
     <string name="tool_permission_allow_5_minutes">允许 5 分钟</string>
     <string name="tool_permission_deny">拒绝</string>
